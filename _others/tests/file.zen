{
    x UInt64 := 14 if !Bool(0) else 15
    y := x + -7

    z UInt64! := x!
    zp := z!
    zp + 17

    v Int := 5
    v1 Int? := null
    v2 := v?

    (v1 ?? v2)!! + 2

    heap Float64#? := null
    anyref Float64? := heap#?
    // fails heap2 Float64#? := anyref#?

    t := alloc?(16)
    dealloc(t#?)
}
